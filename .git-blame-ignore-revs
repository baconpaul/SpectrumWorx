# Commits that moved every line and changed no behaviour.
#
# `git blame` walks past these, so a whitespace sweep does not become the last
# author of the whole tree. Enable it once per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub and GitLab read this file by name and need no configuring.
#
# Only add a commit here if it is *provably* format-only. A commit that
# reformats and fixes something is neither, and belongs in neither list.

# Format the tree with clang-format 21.1.5 (05.08.2026).
# 52 files, 51 of them under src/le/. See .clang-format for the version pin.
5283ba3a453e1de5e5670bf33f84744ba7417eae
